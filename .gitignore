# Xcode project — regenerated by `xcodegen generate`, never hand-edit or commit.
*.xcodeproj
*.xcworkspace
xcuserdata/
*.xcuserstate

# Build output.
build/
DerivedData/
*.ipa
Payload/

# macOS / editor cruft.
.DS_Store
*.swp
*.xcuserdatad/

# Secrets / local env (never commit).
.env
.env.*
*.p12
*.mobileprovision
AuthKey_*.p8

# Optional local OpenCV drop-in (large binary).
**/opencv2.xcframework/

# Local agent / IDE.
.claude/

